# Notas:

Primeros Pasos en Node Js
```
Options:
      --help     Show help               [boolean]
      --version  Show version number     [boolean]
  -b, --base     Es la base de la tabla de
                 multiplicar   [number] [required]
  -l, --listar   Lista la tabla solicitada en
                 consola[boolean] [default: false]
  -h, --hasta    Es el limite de la tabla de
                 multiplicar[number] [default: 10]
```